@model SalesCobrosGeo.Web.Models.Sales.CollectorPortfolioViewModel
@{
    string ResolveImage(string? path, string fallback) =>
        string.IsNullOrWhiteSpace(path) ? fallback :
        (path.StartsWith("http", StringComparison.OrdinalIgnoreCase) || path.StartsWith("data:", StringComparison.OrdinalIgnoreCase) || path.StartsWith("/", StringComparison.OrdinalIgnoreCase)
            ? path
            : "/" + path);

    string StatusAccent(string status) => status.ToUpperInvariant() switch
    {
        "AL CORRIENTE" => "green",
        "ATRASADO" => "red",
        "PASAR MANANA" => "orange",
        "PROXIMA SEMANA" => "pink",
        _ => string.Empty
    };

    string CleanPhone(string? phone) => string.IsNullOrWhiteSpace(phone) ? string.Empty : new string(phone.Where(char.IsDigit).ToArray());
}

<div class="collector-mobile-list collector-mobile-sales" id="collectorMobileSalesList">
    @foreach (var item in Model.Sales)
    {
        var photo = string.IsNullOrWhiteSpace(item.FotoCliente)
            ? ResolveImage(item.FotoFachada, "https://placehold.co/160x120?text=Venta")
            : ResolveImage(item.FotoCliente, "https://placehold.co/160x120?text=Venta");
        var cleanPhone = CleanPhone(item.Celular);
        var mapLink = string.IsNullOrWhiteSpace(item.Coordenadas) ? "#" : $"https://maps.google.com/?q={Uri.EscapeDataString(item.Coordenadas)}";
        var searchText = $"{item.NombreCliente} {item.Zona} {item.NumVenta} {item.DiaCobroPrevisto} {item.Estatus}".ToLowerInvariant();
        var accent = StatusAccent(item.Estatus);
        <article class="cmsr-card @accent" data-search="@searchText">
            <a class="cmsr-link" asp-action="Register" asp-route-id="@item.IdV" asp-route-profile="@Model.Profile" asp-route-day="@Model.SelectedDay" asp-route-status="@Model.SelectedStatus" asp-route-zone="@Model.SelectedZone">
                <img class="cmsr-thumb" src="@photo" alt="@item.NombreCliente" loading="lazy" decoding="async" />
                <div class="cmsr-body">
                    <div class="cmsr-head">
                        <div class="cmsr-name">@item.NombreCliente</div>
                        <span class="cmsr-state @accent">@item.Estatus</span>
                    </div>
                    <div class="cmsr-zone">@item.Zona&ensp;·&ensp;@item.DiaCobroPrevisto</div>
                    <div class="cmsr-foot">
                        <span class="cmsr-id">#@item.NumVenta</span>
                        <span class="cmsr-amount">Bs @item.ImporteRestante.ToString("0,0.##")</span>
                    </div>
                </div>
                <svg class="cmsr-chevron" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true"><path fill-rule="evenodd" d="M8.22 5.22a.75.75 0 011.06 0l4.25 4.25a.75.75 0 010 1.06l-4.25 4.25a.75.75 0 01-1.06-1.06L11.94 10 8.22 6.28a.75.75 0 010-1.06z" clip-rule="evenodd"/></svg>
            </a>
            <div class="cmsr-actions">
                @if (!string.IsNullOrWhiteSpace(cleanPhone))
                {
                    <a href="tel:@cleanPhone" class="cma-btn cma-call" title="Llamar" aria-label="Llamar">
                        <svg viewBox="0 0 20 20" fill="currentColor" aria-hidden="true"><path fill-rule="evenodd" d="M2 3.5A1.5 1.5 0 013.5 2h1.148a1.5 1.5 0 011.465 1.175l.716 3.223a1.5 1.5 0 01-1.052 1.767l-.933.267c-.41.117-.643.555-.48.95a11.542 11.542 0 006.254 6.254c.395.163.833-.07.95-.48l.267-.933a1.5 1.5 0 011.767-1.052l3.223.716A1.5 1.5 0 0118 15.352V16.5a1.5 1.5 0 01-1.5 1.5H15c-1.149 0-2.263-.15-3.326-.43A13.022 13.022 0 012.43 8.326 13.019 13.019 0 012 5V3.5z" clip-rule="evenodd"/></svg>
                    </a>
                }
                <a href="@mapLink" target="_blank" rel="noopener" class="cma-btn cma-map @(mapLink == "#" ? "disabled" : string.Empty)" title="Ver mapa" aria-label="Abrir mapa">
                    <svg viewBox="0 0 20 20" fill="currentColor" aria-hidden="true"><path fill-rule="evenodd" d="M9.69 18.933l.003.001C9.89 19.02 10 19 10 19s.11.02.308-.066l.002-.001.006-.003.018-.008a5.741 5.741 0 00.281-.14c.186-.096.446-.24.757-.433.62-.384 1.445-.966 2.274-1.765C15.302 14.988 17 12.493 17 9A7 7 0 103 9c0 3.492 1.698 5.988 3.355 7.584a13.731 13.731 0 002.273 1.765 11.842 11.842 0 00.976.544l.062.029.018.008.006.003zM10 11.25a2.25 2.25 0 100-4.5 2.25 2.25 0 000 4.5z" clip-rule="evenodd"/></svg>
                </a>
                <a class="cmsr-entry-btn" asp-action="Register" asp-route-id="@item.IdV" asp-route-profile="@Model.Profile" asp-route-day="@Model.SelectedDay" asp-route-status="@Model.SelectedStatus" asp-route-zone="@Model.SelectedZone">
                    Registrar cobro
                    <svg viewBox="0 0 20 20" fill="currentColor" aria-hidden="true"><path d="M2.695 14.763l-1.262 3.154a.5.5 0 00.65.65l3.155-1.262a4 4 0 001.343-.885L17.5 5.5a2.121 2.121 0 00-3-3L3.58 13.42a4 4 0 00-.885 1.343z"/></svg>
                </a>
            </div>
        </article>
    }
</div>
<div class="collector-mobile-emptysearch" id="collectorMobileEmptySearch" hidden>
    <strong>No encontramos coincidencias.</strong>
    <span>Prueba con otro nombre, zona o numero de venta.</span>
</div>
